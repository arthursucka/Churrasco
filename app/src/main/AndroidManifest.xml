<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Churrasco">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela principal (após login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Criar churrasco -->
        <activity
            android:name=".CreateChurrascoActivity1"
            android:exported="false" />
        <activity
            android:name=".CreateChurrascoActivity2"
            android:exported="false" />

        <!-- Detalhes do churrasco para convidado -->
        <activity
            android:name=".ChurrascoDetailsActivity"
            android:exported="false" />

        <!-- Seleção de itens ao confirmar presença -->
        <activity
            android:name=".EscolherItensActivity"
            android:exported="false" />

        <!-- Listagens -->
        <activity
            android:name=".ActiveChurrascosActivity"
            android:exported="false" />
        <activity
            android:name=".PastChurrascosActivity"
            android:exported="false" />
        <activity
            android:name=".ActiveChurrascoDetailsActivity"
            android:exported="false" />

        <!-- Convites pendentes -->
        <activity
            android:name=".PendingInvitesActivity"
            android:exported="false" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Configurações do Firebase -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>
</manifest>
